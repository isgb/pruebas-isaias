import { StyleSheet } from 'react-native'

const selectStyle = StyleSheet.create({
    select: {
        width: '100%',
        backgroundColor: '#fff',
        borderRadius: 30,
        borderColor: '#666',
        borderWidth: 1,
        paddingLeft: 10,
        marginBottom: 10,
    }
});

export default selectStyle